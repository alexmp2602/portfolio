---
import LabCard from "./LabCard.astro";

// Solo mostramos SIGJ y el acceso a la carpeta GSAP
const featuredDemos = [
  {
    slug: "sigj",
    title: "SIGJ | Gestión Judicial",
    description:
      "Demo funcional de un sistema de gestión judicial moderno con interfaz clara y fluida.",
    image: "/assets/lab/sigj.webp",
  },
  {
    slug: "gsap",
    title: "Animaciones con GSAP",
    description:
      "Colección de animaciones y efectos visuales interactivos con GSAP.",
    image: "/assets/lab/gsap-cover.webp",
  },
];
---

<section id="lab" class="page-bg py-24 px-6" data-reveal>
  <div class="max-w-7xl mx-auto">
    <h2
      class="text-4xl md:text-5xl font-extrabold text-center mb-16 text-surface animate-fade-slide"
    >
      Laboratorio Creativo 🧪
    </h2>

    <div
      class="glass-box max-w-3xl mx-auto p-8 mb-12 text-center animate-fade-slide-down"
    >
      <p class="text-lg md:text-xl leading-relaxed text-surface-muted">
        Exploro ideas, animaciones, prototipos visuales y pruebas de código que
        no siempre terminan en proyectos formales, pero muestran cómo
        experimento y aprendo en el proceso.
      </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-8">
      {
        featuredDemos.map((demo, i) => (
          <div
            style={`animation-delay: ${i * 100}ms`}
            class="motion-safe:animate-fade-slide-up"
          >
            <LabCard {...demo} />
          </div>
        ))
      }
    </div>

    <div class="mt-12 text-center">
      <a href="/lab" class="btn-primary rounded-full">
        Ver todos los experimentos
      </a>
    </div>
  </div>
</section>
