---
import LabCard from "./LabCard.astro";

const featuredDemos = [
  {
    slug: "sigj",
    title: "SIGJ | Gestión Judicial",
    description:
      "Demo funcional de un sistema de gestión judicial con interfaz clara y fluida.",
    image: "/assets/lab/sigj.webp",
  },
  {
    slug: "gsap",
    title: "Animaciones con GSAP",
    description:
      "Colección de animaciones y efectos visuales interactivos con GSAP.",
    image: "/assets/lab/gsap-cover.webp",
  },
];
---

<section
  id="laboratorio"
  class="page-bg py-28 px-6 md:px-12"
  role="region"
  aria-labelledby="lab-title"
  data-reveal
>
  <div class="max-w-7xl mx-auto">
    <h2
      id="lab-title"
      class="text-4xl md:text-5xl font-extrabold text-center mb-16 text-surface animate-fade-slide"
    >
      Laboratorio Creativo 🧪
    </h2>

    <div
      class="glass-box max-w-3xl mx-auto p-8 md:p-10 mb-16 text-center animate-fade-slide-down"
    >
      <p
        class="text-lg md:text-xl text-surface-muted leading-relaxed text-balance"
      >
        Aquí comparto ideas, animaciones, prototipos y pequeños experimentos
        visuales. Aunque no siempre evolucionan a proyectos completos, muestran
        cómo aprendo, pruebo y creo constantemente.
      </p>
    </div>

    <ul
      class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-8"
      aria-labelledby="lab-title"
    >
      {
        featuredDemos.map((demo, i) => (
          <li
            class="motion-safe:animate-fade-slide-up"
            style={`animation-delay: ${i * 100}ms`}
          >
            <LabCard {...demo} />
          </li>
        ))
      }
    </ul>

    <div class="mt-16 text-center">
      <a
        href="/lab"
        class="btn-primary rounded-full"
        aria-label="Ver todos los experimentos del laboratorio"
      >
        Ver todos los experimentos
      </a>
    </div>
  </div>
</section>
