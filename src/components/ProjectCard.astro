---
const { title, description, slug, image } = Astro.props;
---

<article
  class="reveal-card bg-white/70 dark:bg-white/5 backdrop-blur-sm rounded-2xl overflow-hidden shadow-md transition-transform hover:scale-[1.015] hover:shadow-lg flex flex-col"
  data-reveal
>
  <a href={`/projects/${slug}`} class="block flex-grow h-full">
    <img
      src={image}
      alt={`Imagen del proyecto ${title}`}
      class="w-full h-48 object-cover rounded-t-2xl"
      loading="lazy"
    />
    <div class="p-5 flex flex-col flex-grow">
      <h3 class="text-xl font-semibold text-dark dark:text-white mb-2">
        {title}
      </h3>
      <p class="text-sm text-neutral-700 dark:text-neutral-300 flex-grow">
        {description}
      </p>
    </div>
  </a>
</article>
