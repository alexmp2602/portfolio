<section data-reveal class="relative text-center space-y-12 max-w-4xl mx-auto">
  <!-- Fondo con blur suave -->
  <div
    class="absolute inset-0 -z-10 bg-gradient-to-tl from-primary/20 to-accent/20 blur-2xl opacity-30 rounded-xl"
  >
  </div>

  <!-- Subtítulo -->
  <p
    class="text-base md:text-lg text-dark/80 dark:text-light/70 max-w-xl mx-auto"
  >
    Estoy disponible para colaborar en proyectos web, desarrollos personalizados
    o ideas creativas. Completá el formulario y te respondo lo antes posible.
  </p>

  <!-- Formulario -->
  <div
    class="bg-light/60 dark:bg-dark/60 backdrop-blur-lg rounded-2xl shadow-xl ring-1 ring-black/5 dark:ring-white/10 p-8 md:p-12 text-left"
  >
    <form
      action="https://formspree.io/f/xyzabcd"
      method="POST"
      class="space-y-6"
    >
      <input
        type="hidden"
        name="_redirect"
        value="https://www.alexpereyra.dev/thanks"
      />
      <div class="hidden">
        <label>
          Don’t fill this out: <input name="bot-field" />
        </label>
      </div>

      <div>
        <label class="block mb-1 text-sm font-medium text-dark dark:text-light">
          Nombre
        </label>
        <input
          type="text"
          name="name"
          required
          class="w-full rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-dark text-dark dark:text-light px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary transition"
        />
      </div>

      <div>
        <label class="block mb-1 text-sm font-medium text-dark dark:text-light">
          Correo electrónico
        </label>
        <input
          type="email"
          name="email"
          required
          class="w-full rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-dark text-dark dark:text-light px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary transition"
        />
      </div>

      <div>
        <label class="block mb-1 text-sm font-medium text-dark dark:text-light">
          Mensaje
        </label>
        <textarea
          name="message"
          required
          rows="5"
          class="w-full rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-dark text-dark dark:text-light px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary transition"
        ></textarea>
      </div>

      <div class="text-center pt-4">
        <button
          type="submit"
          class="inline-block bg-primary hover:bg-accent text-white font-semibold px-6 py-3 rounded-full transition-shadow shadow-md hover:shadow-lg"
        >
          Enviar mensaje
        </button>
      </div>
    </form>
  </div>
</section>
